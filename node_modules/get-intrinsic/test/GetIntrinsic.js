'use strict';

var GetIntrinsic = require('../');

var test = require('tape');
var forEach = require('foreach');
var debug = require('object-inspect');
var generatorFns = require('make-generator-function')();
var asyncFns = require('make-async-function').list();
var asyncGenFns = require('make-async-generator-function')();

var callBound = require('call-bind/callBound');
var v = require('es-value-fixtures');
var $gOPD = require('es-abstract/helpers/getOwnPropertyDescriptor');
var defineProperty = require('es-abstract/test/helpers/defineProperty');

var $isProto = callBound('%Object.prototype.isPrototypeOf%');

test('export', function (t) ***REMOVED***
	t.equal(typeof GetIntrinsic, 'function', 'it is a function');
	t.equal(GetIntrinsic.length, 2, 'function has length of 2');

	t.end();
***REMOVED***);

test('throws', function (t) ***REMOVED***
	t['throws'](
		function () ***REMOVED*** GetIntrinsic('not an intrinsic'); ***REMOVED***,
		SyntaxError,
		'nonexistent intrinsic throws a syntax error'
	);

	t['throws'](
		function () ***REMOVED*** GetIntrinsic(''); ***REMOVED***,
		TypeError,
		'empty string intrinsic throws a type error'
	);

	t['throws'](
		function () ***REMOVED*** GetIntrinsic('.'); ***REMOVED***,
		SyntaxError,
		'"just a dot" intrinsic throws a syntax error'
	);

	t['throws'](
		function () ***REMOVED*** GetIntrinsic('%String'); ***REMOVED***,
		SyntaxError,
		'Leading % without trailing % throws a syntax error'
	);

	t['throws'](
		function () ***REMOVED*** GetIntrinsic('String%'); ***REMOVED***,
		SyntaxError,
		'Trailing % without leading % throws a syntax error'
	);

	t['throws'](
		function () ***REMOVED*** GetIntrinsic("String['prototype]"); ***REMOVED***,
		SyntaxError,
		'Dynamic property access is disallowed for intrinsics (unterminated string)'
	);

	t['throws'](
		function () ***REMOVED*** GetIntrinsic('%Proxy.prototype.undefined%'); ***REMOVED***,
		TypeError,
		"Throws when middle part doesn't exist (%Proxy.prototype.undefined%)"
	);

	forEach(v.nonStrings, function (nonString) ***REMOVED***
		t['throws'](
			function () ***REMOVED*** GetIntrinsic(nonString); ***REMOVED***,
			TypeError,
			debug(nonString) + ' is not a String'
		);
	***REMOVED***);

	forEach(v.nonBooleans, function (nonBoolean) ***REMOVED***
		t['throws'](
			function () ***REMOVED*** GetIntrinsic('%', nonBoolean); ***REMOVED***,
			TypeError,
			debug(nonBoolean) + ' is not a Boolean'
		);
	***REMOVED***);

	forEach([
		'toString',
		'propertyIsEnumerable',
		'hasOwnProperty'
	], function (objectProtoMember) ***REMOVED***
		t['throws'](
			function () ***REMOVED*** GetIntrinsic(objectProtoMember); ***REMOVED***,
			SyntaxError,
			debug(objectProtoMember) + ' is not an intrinsic'
		);
	***REMOVED***);

	t.end();
***REMOVED***);

test('base intrinsics', function (t) ***REMOVED***
	t.equal(GetIntrinsic('%Object%'), Object, '%Object% yields Object');
	t.equal(GetIntrinsic('Object'), Object, 'Object yields Object');
	t.equal(GetIntrinsic('%Array%'), Array, '%Array% yields Array');
	t.equal(GetIntrinsic('Array'), Array, 'Array yields Array');

	t.end();
***REMOVED***);

test('dotted paths', function (t) ***REMOVED***
	t.equal(GetIntrinsic('%Object.prototype.toString%'), Object.prototype.toString, '%Object.prototype.toString% yields Object.prototype.toString');
	t.equal(GetIntrinsic('Object.prototype.toString'), Object.prototype.toString, 'Object.prototype.toString yields Object.prototype.toString');
	t.equal(GetIntrinsic('%Array.prototype.push%'), Array.prototype.push, '%Array.prototype.push% yields Array.prototype.push');
	t.equal(GetIntrinsic('Array.prototype.push'), Array.prototype.push, 'Array.prototype.push yields Array.prototype.push');

	test('underscore paths are aliases for dotted paths', ***REMOVED*** skip: !Object.isFrozen || Object.isFrozen(Object.prototype) ***REMOVED***, function (st) ***REMOVED***
		var original = GetIntrinsic('%ObjProto_toString%');

		forEach([
			'%Object.prototype.toString%',
			'Object.prototype.toString',
			'%ObjectPrototype.toString%',
			'ObjectPrototype.toString',
			'%ObjProto_toString%',
			'ObjProto_toString'
		], function (name) ***REMOVED***
			defineProperty(Object.prototype, 'toString', ***REMOVED***
				value: function toString() ***REMOVED***
					return original.apply(this, arguments);
				***REMOVED***
			***REMOVED***);
			st.equal(GetIntrinsic(name), original, name + ' yields original Object.prototype.toString');
		***REMOVED***);

		defineProperty(Object.prototype, 'toString', ***REMOVED*** value: original ***REMOVED***);
		st.end();
	***REMOVED***);

	test('dotted paths cache', ***REMOVED*** skip: !Object.isFrozen || Object.isFrozen(Object.prototype) ***REMOVED***, function (st) ***REMOVED***
		var original = GetIntrinsic('%Object.prototype.propertyIsEnumerable%');

		forEach([
			'%Object.prototype.propertyIsEnumerable%',
			'Object.prototype.propertyIsEnumerable',
			'%ObjectPrototype.propertyIsEnumerable%',
			'ObjectPrototype.propertyIsEnumerable'
		], function (name) ***REMOVED***
			// eslint-disable-next-line no-extend-native
			Object.prototype.propertyIsEnumerable = function propertyIsEnumerable() ***REMOVED***
				return original.apply(this, arguments);
			***REMOVED***;
			st.equal(GetIntrinsic(name), original, name + ' yields cached Object.prototype.propertyIsEnumerable');
		***REMOVED***);

		// eslint-disable-next-line no-extend-native
		Object.prototype.propertyIsEnumerable = original;
		st.end();
	***REMOVED***);

	test('dotted path reports correct error', function (st) ***REMOVED***
		st['throws'](function () ***REMOVED***
			GetIntrinsic('%NonExistentIntrinsic.prototype.property%');
		***REMOVED***, /%NonExistentIntrinsic%/, 'The base intrinsic of %NonExistentIntrinsic.prototype.property% is %NonExistentIntrinsic%');

		st['throws'](function () ***REMOVED***
			GetIntrinsic('%NonExistentIntrinsicPrototype.property%');
		***REMOVED***, /%NonExistentIntrinsicPrototype%/, 'The base intrinsic of %NonExistentIntrinsicPrototype.property% is %NonExistentIntrinsicPrototype%');

		st.end();
	***REMOVED***);

	t.end();
***REMOVED***);

test('accessors', ***REMOVED*** skip: !$gOPD || typeof Map !== 'function' ***REMOVED***, function (t) ***REMOVED***
	var actual = $gOPD(Map.prototype, 'size');
	t.ok(actual, 'Map.prototype.size has a descriptor');
	t.equal(typeof actual.get, 'function', 'Map.prototype.size has a getter function');
	t.equal(GetIntrinsic('%Map.prototype.size%'), actual.get, '%Map.prototype.size% yields the getter for it');
	t.equal(GetIntrinsic('Map.prototype.size'), actual.get, 'Map.prototype.size yields the getter for it');

	t.end();
***REMOVED***);

test('generator functions', ***REMOVED*** skip: !generatorFns.length ***REMOVED***, function (t) ***REMOVED***
	var $GeneratorFunction = GetIntrinsic('%GeneratorFunction%');
	var $GeneratorFunctionPrototype = GetIntrinsic('%Generator%');
	var $GeneratorPrototype = GetIntrinsic('%GeneratorPrototype%');

	forEach(generatorFns, function (genFn) ***REMOVED***
		var fnName = genFn.name;
		fnName = fnName ? "'" + fnName + "'" : 'genFn';

		t.ok(genFn instanceof $GeneratorFunction, fnName + ' instanceof %GeneratorFunction%');
		t.ok($isProto($GeneratorFunctionPrototype, genFn), '%Generator% is prototype of ' + fnName);
		t.ok($isProto($GeneratorPrototype, genFn.prototype), '%GeneratorPrototype% is prototype of ' + fnName + '.prototype');
	***REMOVED***);

	t.end();
***REMOVED***);

test('async functions', ***REMOVED*** skip: !asyncFns.length ***REMOVED***, function (t) ***REMOVED***
	var $AsyncFunction = GetIntrinsic('%AsyncFunction%');
	var $AsyncFunctionPrototype = GetIntrinsic('%AsyncFunctionPrototype%');

	forEach(asyncFns, function (asyncFn) ***REMOVED***
		var fnName = asyncFn.name;
		fnName = fnName ? "'" + fnName + "'" : 'asyncFn';

		t.ok(asyncFn instanceof $AsyncFunction, fnName + ' instanceof %AsyncFunction%');
		t.ok($isProto($AsyncFunctionPrototype, asyncFn), '%AsyncFunctionPrototype% is prototype of ' + fnName);
	***REMOVED***);

	t.end();
***REMOVED***);

test('async generator functions', ***REMOVED*** skip: asyncGenFns.length === 0 ***REMOVED***, function (t) ***REMOVED***
	var $AsyncGeneratorFunction = GetIntrinsic('%AsyncGeneratorFunction%');
	var $AsyncGeneratorFunctionPrototype = GetIntrinsic('%AsyncGenerator%');
	var $AsyncGeneratorPrototype = GetIntrinsic('%AsyncGeneratorPrototype%');

	forEach(asyncGenFns, function (asyncGenFn) ***REMOVED***
		var fnName = asyncGenFn.name;
		fnName = fnName ? "'" + fnName + "'" : 'asyncGenFn';

		t.ok(asyncGenFn instanceof $AsyncGeneratorFunction, fnName + ' instanceof %AsyncGeneratorFunction%');
		t.ok($isProto($AsyncGeneratorFunctionPrototype, asyncGenFn), '%AsyncGenerator% is prototype of ' + fnName);
		t.ok($isProto($AsyncGeneratorPrototype, asyncGenFn.prototype), '%AsyncGeneratorPrototype% is prototype of ' + fnName + '.prototype');
	***REMOVED***);

	t.end();
***REMOVED***);

test('%ThrowTypeError%', function (t) ***REMOVED***
	var $ThrowTypeError = GetIntrinsic('%ThrowTypeError%');

	t.equal(typeof $ThrowTypeError, 'function', 'is a function');
	t['throws'](
		$ThrowTypeError,
		TypeError,
		'%ThrowTypeError% throws a TypeError'
	);

	t.end();
***REMOVED***);

test('allowMissing', ***REMOVED*** skip: asyncGenFns.length > 0 ***REMOVED***, function (t) ***REMOVED***
	t['throws'](
		function () ***REMOVED*** GetIntrinsic('%AsyncGeneratorPrototype%'); ***REMOVED***,
		TypeError,
		'throws when missing'
	);

	t.equal(
		GetIntrinsic('%AsyncGeneratorPrototype%', true),
		undefined,
		'does not throw when allowMissing'
	);

	t.end();
***REMOVED***);
