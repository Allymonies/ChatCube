var express = require('express');
var expressWs = require('..');

var expressWs = expressWs(express());
var app = expressWs.app;

app.param('world', function (req, res, next, world) ***REMOVED***
  req.world = world || 'world';
  return next();
***REMOVED***);

app.get('/hello/:world', function(req, res, next)***REMOVED***
  console.log('hello', req.world);
  res.end();
  next();
***REMOVED***);

app.ws('/hello/:world', function(ws, req, next) ***REMOVED***
  ws.on('message', function(msg) ***REMOVED***
    console.log(msg);
  ***REMOVED***);
  console.log('socket hello', req.world);
  next();
***REMOVED***);

app.listen(3000)
