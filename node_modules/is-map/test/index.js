'use strict';

var test = require('tape');
var debug = require('object-inspect');
var forEach = require('for-each');

var isMap = require('..');

test('non-collections', function (t) ***REMOVED***
	forEach([
		null,
		undefined,
		true,
		false,
		42,
		0,
		-0,
		NaN,
		Infinity,
		'',
		'foo',
		/a/g,
		[],
		***REMOVED******REMOVED***,
		function () ***REMOVED******REMOVED***
	], function (nonCollection) ***REMOVED***
		t.equal(isMap(nonCollection), false, debug(nonCollection) + ' is not a Map');
	***REMOVED***);

	t.end();
***REMOVED***);

test('Maps', ***REMOVED*** skip: typeof Map !== 'function' ***REMOVED***, function (t) ***REMOVED***
	var m = new Map();
	t.equal(isMap(m), true, debug(m) + ' is a Map');

	t.end();
***REMOVED***);

test('Sets', ***REMOVED*** skip: typeof Set !== 'function' ***REMOVED***, function (t) ***REMOVED***
	var s = new Set();
	t.equal(isMap(s), false, debug(s) + ' is not a Map');

	t.end();
***REMOVED***);

test('WeakMaps', ***REMOVED*** skip: typeof WeakMap !== 'function' ***REMOVED***, function (t) ***REMOVED***
	var wm = new WeakMap();
	t.equal(isMap(wm), false, debug(wm) + ' is not a Map');

	t.end();
***REMOVED***);

test('WeakSets', ***REMOVED*** skip: typeof WeakSet !== 'function' ***REMOVED***, function (t) ***REMOVED***
	var ws = new WeakSet();
	t.equal(isMap(ws), false, debug(ws) + ' is not a Map');

	t.end();
***REMOVED***);
