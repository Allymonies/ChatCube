'use strict';

var test = require('tape');
var inspect = require('object-inspect');
var forEach = require('for-each');

var isWeakRef = require('..');

test('isWeakRef', function (t) ***REMOVED***
	t.equal(typeof isWeakRef, 'function', 'is a function');

	var nonWeakRefs = [undefined, null, true, false, 42, 0, Infinity, NaN, /a/g, function () ***REMOVED******REMOVED***, ***REMOVED******REMOVED***, []];
	forEach(nonWeakRefs, function (nonWeakRef) ***REMOVED***
		t.equal(isWeakRef(nonWeakRef), false, inspect(nonWeakRef) + ' is not a WeakRef');
	***REMOVED***);

	t.test('actual WeakRefs', ***REMOVED*** skip: typeof WeakRef === 'undefined' ***REMOVED***, function (st) ***REMOVED***
		var ref = new WeakRef(***REMOVED******REMOVED***);

		st.equal(isWeakRef(ref), true, inspect(ref) + ' is a WeakRef');

		st.end();
	***REMOVED***);

	t.end();
***REMOVED***);
