'use strict';

var $TypeError = TypeError;

// eslint-disable-next-line consistent-return
module.exports = function iterateIterator(iterator) ***REMOVED***
	if (!iterator || typeof iterator.next !== 'function') ***REMOVED***
		throw new $TypeError('iterator must be an object with a `next` method');
	***REMOVED***
	if (arguments.length > 1) ***REMOVED***
		var callback = arguments[1];
		if (typeof callback !== 'function') ***REMOVED***
			throw new $TypeError('`callback`, if provided, must be a function');
		***REMOVED***
	***REMOVED***
	var values = callback || [];
	var result;
	while ((result = iterator.next()) && !result.done) ***REMOVED***
		if (callback) ***REMOVED***
			callback(result.value); // eslint-disable-line callback-return
		***REMOVED*** else ***REMOVED***
			values.push(result.value);
		***REMOVED***
	***REMOVED***
	if (!callback) ***REMOVED***
		return values;
	***REMOVED***
***REMOVED***;
