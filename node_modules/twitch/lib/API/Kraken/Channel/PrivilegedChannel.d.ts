import type ***REMOVED*** CommercialLength ***REMOVED*** from '../../Shared/CommercialLength';
import type ***REMOVED*** User ***REMOVED*** from '../User/User';
import type ***REMOVED*** ChannelData ***REMOVED*** from './Channel';
import ***REMOVED*** Channel ***REMOVED*** from './Channel';
/** @private */
export interface PrivilegedChannelData extends ChannelData ***REMOVED***
    stream_key: string;
    email: string;
***REMOVED***
/**
 * A channel you have extended privileges for, i.e. the channel of the currently authenticated user.
 */
export declare class PrivilegedChannel extends Channel ***REMOVED***
    /** @private */ protected readonly _data: PrivilegedChannelData;
    /**
     * The channel's stream key.
     */
    get streamKey(): string;
    /**
     * The channel's email address.
     */
    get email(): string;
    /**
     * Retrieves the list of editors of the channel.
     */
    getEditors(): Promise<User[]>;
    /**
     * Starts a commercial in the channel.
     *
     * @param length The length of the commercial.
     */
    startCommercial(length: CommercialLength): Promise<void>;
    /**
     * Resets the given channel's stream key.
     */
    resetStreamKey(): Promise<string>;
***REMOVED***
