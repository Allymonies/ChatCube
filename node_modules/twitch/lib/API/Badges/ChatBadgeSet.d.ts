import type ***REMOVED*** ChatBadgeVersionData ***REMOVED*** from './ChatBadgeVersion';
import ***REMOVED*** ChatBadgeVersion ***REMOVED*** from './ChatBadgeVersion';
/** @private */
export interface ChatBadgeSetData ***REMOVED***
    versions: Record<string, ChatBadgeVersionData>;
***REMOVED***
/**
 * A set of badges.
 */
export declare class ChatBadgeSet ***REMOVED***
    private readonly _data;
    /** @private */
    constructor(data: ChatBadgeSetData);
    /**
     * Names of all versions of the badge set.
     */
    get versionNames(): string[];
    /**
     * Gets a specific version of a badge by name.
     *
     * @param name The name of the version.
     */
    getVersion(name: string): ChatBadgeVersion;
***REMOVED***
