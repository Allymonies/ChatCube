'use strict';

var test = require('tape');
var hasBigInts = require('..');

test('interface', function (t) ***REMOVED***
	t.equal(typeof hasBigInts, 'function', 'is a function');
	t.equal(typeof hasBigInts(), 'boolean', 'returns a boolean');
	t.end();
***REMOVED***);

test('BigInts are supported', ***REMOVED*** skip: !hasBigInts() ***REMOVED***, function (t) ***REMOVED***
	t.equal(typeof BigInt, 'function', 'global BigInt is a function');
	if (typeof BigInt !== 'function') ***REMOVED***
		return;
	***REMOVED***

	t.equal(BigInt(42), BigInt(42), '42n === 42n');
	t['throws'](
		function () ***REMOVED*** BigInt(NaN); ***REMOVED***,
		RangeError,
		'NaN is not an integer; BigInt(NaN) throws'
	);

	t['throws'](
		function () ***REMOVED*** BigInt(Infinity); ***REMOVED***,
		RangeError,
		'Infinity is not an integer; BigInt(Infinity) throws'
	);

	t['throws'](
		function () ***REMOVED*** BigInt(1.1); ***REMOVED***,
		RangeError,
		'1.1 is not an integer; BigInt(1.1) throws'
	);

	t.end();
***REMOVED***);

test('BigInts are not supported', ***REMOVED*** skip: hasBigInts() ***REMOVED***, function (t) ***REMOVED***
	t.equal(typeof BigInt, 'undefined', 'global BigInt is undefined');

	t.end();
***REMOVED***);
