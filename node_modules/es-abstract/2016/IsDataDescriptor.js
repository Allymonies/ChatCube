'use strict';

var has = require('has');

var assertRecord = require('../helpers/assertRecord');

var Type = require('./Type');

// https://ecma-international.org/ecma-262/6.0/#sec-isdatadescriptor

module.exports = function IsDataDescriptor(Desc) ***REMOVED***
	if (typeof Desc === 'undefined') ***REMOVED***
		return false;
	***REMOVED***

	assertRecord(Type, 'Property Descriptor', 'Desc', Desc);

	if (!has(Desc, '[[Value]]') && !has(Desc, '[[Writable]]')) ***REMOVED***
		return false;
	***REMOVED***

	return true;
***REMOVED***;
