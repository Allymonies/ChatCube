'use strict';

var trimStart = require('string.prototype.trimstart');
var trimEnd = require('string.prototype.trimend');

var GetIntrinsic = require('get-intrinsic');

var $TypeError = GetIntrinsic('%TypeError%');

var RequireObjectCoercible = require('./RequireObjectCoercible');
var ToString = require('./ToString');

// https://262.ecma-international.org/10.0/#sec-trimstring

module.exports = function TrimString(string, where) ***REMOVED***
	var str = RequireObjectCoercible(string);
	var S = ToString(str);
	var T;
	if (where === 'start') ***REMOVED***
		T = trimStart(S);
	***REMOVED*** else if (where === 'end') ***REMOVED***
		T = trimEnd(S);
	***REMOVED*** else if (where === 'start+end') ***REMOVED***
		T = trimStart(trimEnd(S));
	***REMOVED*** else ***REMOVED***
		throw new $TypeError('Assertion failed: invalid `where` value; must be "start", "end", or "start+end"');
	***REMOVED***
	return T;
***REMOVED***;
