'use strict';

var GetIntrinsic = require('get-intrinsic');

var $TypeError = GetIntrinsic('%TypeError%');

var IsArray = require('./IsArray');

var isByteValue = require('../helpers/isByteValue');

// https://ecma-international.org/ecma-262/12.0/#sec-bytelistequal

module.exports = function ByteListEqual(xBytes, yBytes) ***REMOVED***
	if (!IsArray(xBytes) || !IsArray(yBytes)) ***REMOVED***
		throw new $TypeError('Assertion failed: `xBytes` and `yBytes` must be sequences of byte values (an integer 0-255, inclusive)');
	***REMOVED***

	if (xBytes.length !== yBytes.length) ***REMOVED***
		return false;
	***REMOVED***

	for (var i = 0; i < xBytes.length; i += 1) ***REMOVED***
		var xByte = xBytes[i];
		var yByte = yBytes[i];
		if (!isByteValue(xByte) || !isByteValue(yByte)) ***REMOVED***
			throw new $TypeError('Assertion failed: `xBytes` and `yBytes` must be sequences of byte values (an integer 0-255, inclusive)');
		***REMOVED***
		if (xByte !== yByte) ***REMOVED***
			return false;
		***REMOVED***
	***REMOVED***
	return true;
***REMOVED***;
