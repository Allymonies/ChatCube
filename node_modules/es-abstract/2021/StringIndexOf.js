'use strict';

var GetIntrinsic = require('get-intrinsic');
var callBound = require('call-bind/callBound');

var $TypeError = GetIntrinsic('%TypeError%');

var IsIntegralNumber = require('./IsIntegralNumber');
var Type = require('./Type');

var $slice = callBound('String.prototype.slice');

// https://ecma-international.org/ecma-262/12.0/#sec-stringindexof

module.exports = function StringIndexOf(string, searchValue, fromIndex) ***REMOVED***
	if (Type(string) !== 'String') ***REMOVED***
		throw new $TypeError('Assertion failed: `string` must be a String');
	***REMOVED***
	if (Type(searchValue) !== 'String') ***REMOVED***
		throw new $TypeError('Assertion failed: `searchValue` must be a String');
	***REMOVED***
	if (!IsIntegralNumber(fromIndex) || fromIndex < 0) ***REMOVED***
		throw new $TypeError('Assertion failed: `fromIndex` must be a non-negative integer');
	***REMOVED***

	var len = string.length;
	if (searchValue === '' && fromIndex <= len) ***REMOVED***
		return fromIndex;
	***REMOVED***

	var searchLen = searchValue.length;
	for (var i = fromIndex; i <= (len - searchLen); i += 1) ***REMOVED***
		var candidate = $slice(string, i, i + searchLen);
		if (candidate === searchValue) ***REMOVED***
			return i;
		***REMOVED***
	***REMOVED***
	return -1;
***REMOVED***;
