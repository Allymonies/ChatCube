'use strict';

var MAX_SAFE_INTEGER = require('../helpers/maxSafeInteger');

var ToIntegerOrInfinity = require('./ToIntegerOrInfinity');

module.exports = function ToLength(argument) ***REMOVED***
	var len = ToIntegerOrInfinity(argument);
	if (len <= 0) ***REMOVED*** return 0; ***REMOVED*** // includes converting -0 to +0
	if (len > MAX_SAFE_INTEGER) ***REMOVED*** return MAX_SAFE_INTEGER; ***REMOVED***
	return len;
***REMOVED***;
