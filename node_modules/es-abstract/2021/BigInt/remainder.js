'use strict';

var GetIntrinsic = require('get-intrinsic');

var $BigInt = GetIntrinsic('%BigInt%', true);
var $RangeError = GetIntrinsic('%RangeError%');
var $TypeError = GetIntrinsic('%TypeError%');

var Type = require('../Type');

var zero = $BigInt && $BigInt(0);

// https://262.ecma-international.org/11.0/#sec-numeric-types-bigint-remainder

module.exports = function BigIntRemainder(n, d) ***REMOVED***
	if (Type(n) !== 'BigInt' || Type(d) !== 'BigInt') ***REMOVED***
		throw new $TypeError('Assertion failed: `n` and `d` arguments must be BigInts');
	***REMOVED***

	if (d === zero) ***REMOVED***
		throw new $RangeError('Division by zero');
	***REMOVED***

	if (n === zero) ***REMOVED***
		return zero;
	***REMOVED***

	// shortcut for the actual spec mechanics
	return n % d;
***REMOVED***;
