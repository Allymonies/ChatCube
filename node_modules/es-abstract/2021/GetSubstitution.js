'use strict';

var GetIntrinsic = require('get-intrinsic');

var $TypeError = GetIntrinsic('%TypeError%');

var callBound = require('call-bind/callBound');
var regexTester = require('../helpers/regexTester');
var every = require('../helpers/every');

var $charAt = callBound('String.prototype.charAt');
var $strSlice = callBound('String.prototype.slice');
var $indexOf = callBound('String.prototype.indexOf');
var $parseInt = parseInt;

var isDigit = regexTester(/^[0-9]$/);

var inspect = require('object-inspect');

var Get = require('./Get');
var IsArray = require('./IsArray');
var IsIntegralNumber = require('./IsIntegralNumber');
var ToObject = require('./ToObject');
var ToString = require('./ToString');
var Type = require('./Type');

var canDistinguishSparseFromUndefined = 0 in [undefined]; // IE 6 - 8 have a bug where this returns false

var isStringOrHole = function (capture, index, arr) ***REMOVED***
	return Type(capture) === 'String' || (canDistinguishSparseFromUndefined ? !(index in arr) : Type(capture) === 'Undefined');
***REMOVED***;

// http://www.ecma-international.org/ecma-262/12.0/#sec-getsubstitution

// eslint-disable-next-line max-statements, max-params, max-lines-per-function
module.exports = function GetSubstitution(matched, str, position, captures, namedCaptures, replacement) ***REMOVED***
	if (Type(matched) !== 'String') ***REMOVED***
		throw new $TypeError('Assertion failed: `matched` must be a String');
	***REMOVED***
	var matchLength = matched.length;

	if (Type(str) !== 'String') ***REMOVED***
		throw new $TypeError('Assertion failed: `str` must be a String');
	***REMOVED***
	var stringLength = str.length;

	if (!IsIntegralNumber(position) || position < 0 || position > stringLength) ***REMOVED***
		throw new $TypeError('Assertion failed: `position` must be a nonnegative integer, and less than or equal to the length of `string`, got ' + inspect(position));
	***REMOVED***

	if (!IsArray(captures) || !every(captures, isStringOrHole)) ***REMOVED***
		throw new $TypeError('Assertion failed: `captures` must be a possibly-empty List of Strings, got ' + inspect(captures));
	***REMOVED***

	if (Type(replacement) !== 'String') ***REMOVED***
		throw new $TypeError('Assertion failed: `replacement` must be a String');
	***REMOVED***

	var tailPos = position + matchLength;
	var m = captures.length;
	if (Type(namedCaptures) !== 'Undefined') ***REMOVED***
		namedCaptures = ToObject(namedCaptures); // eslint-disable-line no-param-reassign
	***REMOVED***

	var result = '';
	for (var i = 0; i < replacement.length; i += 1) ***REMOVED***
		// if this is a $, and it's not the end of the replacement
		var current = $charAt(replacement, i);
		var isLast = (i + 1) >= replacement.length;
		var nextIsLast = (i + 2) >= replacement.length;
		if (current === '$' && !isLast) ***REMOVED***
			var next = $charAt(replacement, i + 1);
			if (next === '$') ***REMOVED***
				result += '$';
				i += 1;
			***REMOVED*** else if (next === '&') ***REMOVED***
				result += matched;
				i += 1;
			***REMOVED*** else if (next === '`') ***REMOVED***
				result += position === 0 ? '' : $strSlice(str, 0, position - 1);
				i += 1;
			***REMOVED*** else if (next === "'") ***REMOVED***
				result += tailPos >= stringLength ? '' : $strSlice(str, tailPos);
				i += 1;
			***REMOVED*** else ***REMOVED***
				var nextNext = nextIsLast ? null : $charAt(replacement, i + 2);
				if (isDigit(next) && next !== '0' && (nextIsLast || !isDigit(nextNext))) ***REMOVED***
					// $1 through $9, and not followed by a digit
					var n = $parseInt(next, 10);
					// if (n > m, impl-defined)
					result += n <= m && Type(captures[n - 1]) === 'Undefined' ? '' : captures[n - 1];
					i += 1;
				***REMOVED*** else if (isDigit(next) && (nextIsLast || isDigit(nextNext))) ***REMOVED***
					// $00 through $99
					var nn = next + nextNext;
					var nnI = $parseInt(nn, 10) - 1;
					// if nn === '00' or nn > m, impl-defined
					result += nn <= m && Type(captures[nnI]) === 'Undefined' ? '' : captures[nnI];
					i += 2;
				***REMOVED*** else if (next === '<') ***REMOVED***
					// eslint-disable-next-line max-depth
					if (Type(namedCaptures) === 'Undefined') ***REMOVED***
						result += '$<';
						i += 2;
					***REMOVED*** else ***REMOVED***
						var endIndex = $indexOf(replacement, '>', i);
						// eslint-disable-next-line max-depth
						if (endIndex > -1) ***REMOVED***
							var groupName = $strSlice(replacement, i + '$<'.length, endIndex);
							var capture = Get(namedCaptures, groupName);
							// eslint-disable-next-line max-depth
							if (Type(capture) !== 'Undefined') ***REMOVED***
								result += ToString(capture);
							***REMOVED***
							i += ('<' + groupName + '>').length;
						***REMOVED***
					***REMOVED***
				***REMOVED*** else ***REMOVED***
					result += '$';
				***REMOVED***
			***REMOVED***
		***REMOVED*** else ***REMOVED***
			// the final $, or else not a $
			result += $charAt(replacement, i);
		***REMOVED***
	***REMOVED***
	return result;
***REMOVED***;
