'use strict';

var GetIntrinsic = require('get-intrinsic');

var $TypeError = GetIntrinsic('%TypeError%');

var callBound = require('call-bind/callBound');

var $push = callBound('Array.prototype.push');

var CodePointAt = require('./CodePointAt');
var Type = require('./Type');

// https://ecma-international.org/ecma-262/12.0/#sec-stringtocodepoints

module.exports = function StringToCodePoints(string) ***REMOVED***
	if (Type(string) !== 'String') ***REMOVED***
		throw new $TypeError('Assertion failed: `string` must be a String');
	***REMOVED***
	var codePoints = [];
	var size = string.length;
	var position = 0;
	while (position < size) ***REMOVED***
		var cp = CodePointAt(string, position);
		$push(codePoints, cp['[[CodePoint]]']);
		position += cp['[[CodeUnitCount]]'];
	***REMOVED***
	return codePoints;
***REMOVED***;
