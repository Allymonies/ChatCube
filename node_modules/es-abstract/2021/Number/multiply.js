'use strict';

var GetIntrinsic = require('get-intrinsic');

var $TypeError = GetIntrinsic('%TypeError%');

var isNaN = require('../../helpers/isNaN');

var Type = require('../Type');

// https://262.ecma-international.org/11.0/#sec-numeric-types-number-multiply

module.exports = function NumberMultiply(x, y) ***REMOVED***
	if (Type(x) !== 'Number' || Type(y) !== 'Number') ***REMOVED***
		throw new $TypeError('Assertion failed: `x` and `y` arguments must be Numbers');
	***REMOVED***

	if (isNaN(x) || isNaN(y) || (x === 0 && !isFinite(y)) || (!isFinite(x) && y === 0)) ***REMOVED***
		return NaN;
	***REMOVED***
	if (!isFinite(x) && !isFinite(y)) ***REMOVED***
		return x === y ? Infinity : -Infinity;
	***REMOVED***
	if (!isFinite(x) && y !== 0) ***REMOVED***
		return x > 0 ? Infinity : -Infinity;
	***REMOVED***
	if (!isFinite(y) && x !== 0) ***REMOVED***
		return y > 0 ? Infinity : -Infinity;
	***REMOVED***

	// shortcut for the actual spec mechanics
	return x * y;
***REMOVED***;
