'use strict';

var has = require('has');

var assertRecord = require('../helpers/assertRecord');

var Type = require('./Type');

// https://ecma-international.org/ecma-262/6.0/#sec-isaccessordescriptor

module.exports = function IsAccessorDescriptor(Desc) ***REMOVED***
	if (typeof Desc === 'undefined') ***REMOVED***
		return false;
	***REMOVED***

	assertRecord(Type, 'Property Descriptor', 'Desc', Desc);

	if (!has(Desc, '[[Get]]') && !has(Desc, '[[Set]]')) ***REMOVED***
		return false;
	***REMOVED***

	return true;
***REMOVED***;
