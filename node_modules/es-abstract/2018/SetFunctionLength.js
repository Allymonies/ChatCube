'use strict';

var GetIntrinsic = require('get-intrinsic');

var $TypeError = GetIntrinsic('%TypeError%');

var DefinePropertyOrThrow = require('./DefinePropertyOrThrow');
var HasOwnProperty = require('./HasOwnProperty');
var IsExtensible = require('./IsExtensible');
var IsInteger = require('./IsInteger');
var Type = require('./Type');

// https://262.ecma-international.org/9.0/#sec-setfunctionlength

module.exports = function SetFunctionLength(F, length) ***REMOVED***
	if (typeof F !== 'function' || !IsExtensible(F) || HasOwnProperty(F, 'length')) ***REMOVED***
		throw new $TypeError('Assertion failed: `F` must be an extensible function and lack an own `length` property');
	***REMOVED***
	if (Type(length) !== 'Number') ***REMOVED***
		throw new $TypeError('Assertion failed: `length` must be a Number');
	***REMOVED***
	if (length < 0 || !IsInteger(length)) ***REMOVED***
		throw new $TypeError('Assertion failed: `length` must be an integer >= 0');
	***REMOVED***
	return DefinePropertyOrThrow(F, 'length', ***REMOVED***
		'[[Configurable]]': true,
		'[[Enumerable]]': false,
		'[[Value]]': length,
		'[[Writable]]': false
	***REMOVED***);
***REMOVED***;
