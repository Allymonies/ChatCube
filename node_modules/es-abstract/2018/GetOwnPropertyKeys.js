'use strict';

var GetIntrinsic = require('get-intrinsic');

var hasSymbols = require('has-symbols')();

var $TypeError = GetIntrinsic('%TypeError%');

var $gOPN = GetIntrinsic('%Object.getOwnPropertyNames%');
var $gOPS = hasSymbols && GetIntrinsic('%Object.getOwnPropertySymbols%');
var keys = require('object-keys');

var esType = require('./Type');

// https://ecma-international.org/ecma-262/6.0/#sec-getownpropertykeys

module.exports = function GetOwnPropertyKeys(O, Type) ***REMOVED***
	if (esType(O) !== 'Object') ***REMOVED***
		throw new $TypeError('Assertion failed: Type(O) is not Object');
	***REMOVED***
	if (Type === 'Symbol') ***REMOVED***
		return $gOPS ? $gOPS(O) : [];
	***REMOVED***
	if (Type === 'String') ***REMOVED***
		if (!$gOPN) ***REMOVED***
			return keys(O);
		***REMOVED***
		return $gOPN(O);
	***REMOVED***
	throw new $TypeError('Assertion failed: `Type` must be `"String"` or `"Symbol"`');
***REMOVED***;
