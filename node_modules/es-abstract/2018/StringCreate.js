'use strict';

var GetIntrinsic = require('get-intrinsic');

var $Object = GetIntrinsic('%Object%');
var $StringPrototype = GetIntrinsic('%String.prototype%');
var $SyntaxError = GetIntrinsic('%SyntaxError%');
var $TypeError = GetIntrinsic('%TypeError%');

var DefinePropertyOrThrow = require('./DefinePropertyOrThrow');
var Type = require('./Type');

var setProto = require('../helpers/setProto');

// https://262.ecma-international.org/6.0/#sec-stringcreate

module.exports = function StringCreate(value, prototype) ***REMOVED***
	if (Type(value) !== 'String') ***REMOVED***
		throw new $TypeError('Assertion failed: `S` must be a String');
	***REMOVED***

	var S = $Object(value);
	if (S !== $StringPrototype) ***REMOVED***
		if (setProto) ***REMOVED***
			setProto(S, prototype);
		***REMOVED*** else ***REMOVED***
			throw new $SyntaxError('StringCreate: a `proto` argument that is not `String.prototype` is not supported in an environment that does not support setting the [[Prototype]]');
		***REMOVED***
	***REMOVED***

	var length = value.length;
	DefinePropertyOrThrow(S, 'length', ***REMOVED***
		'[[Configurable]]': false,
		'[[Enumerable]]': false,
		'[[Value]]': length,
		'[[Writable]]': false
	***REMOVED***);

	return S;
***REMOVED***;
