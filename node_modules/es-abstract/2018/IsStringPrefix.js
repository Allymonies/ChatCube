'use strict';

var GetIntrinsic = require('get-intrinsic');

var $TypeError = GetIntrinsic('%TypeError%');

var isPrefixOf = require('../helpers/isPrefixOf');

// var callBound = require('call-bind/callBound');

// var $charAt = callBound('String.prototype.charAt');

var Type = require('./Type');

// https://262.ecma-international.org/9.0/#sec-isstringprefix

module.exports = function IsStringPrefix(p, q) ***REMOVED***
	if (Type(p) !== 'String') ***REMOVED***
		throw new $TypeError('Assertion failed: "p" must be a String');
	***REMOVED***

	if (Type(q) !== 'String') ***REMOVED***
		throw new $TypeError('Assertion failed: "q" must be a String');
	***REMOVED***

	return isPrefixOf(p, q);
	/*
	if (p === q || p === '') ***REMOVED***
		return true;
	***REMOVED***

	var pLength = p.length;
	var qLength = q.length;
	if (pLength >= qLength) ***REMOVED***
		return false;
	***REMOVED***

	// assert: pLength < qLength

	for (var i = 0; i < pLength; i += 1) ***REMOVED***
		if ($charAt(p, i) !== $charAt(q, i)) ***REMOVED***
			return false;
		***REMOVED***
	***REMOVED***
	return true;
	*/
***REMOVED***;
