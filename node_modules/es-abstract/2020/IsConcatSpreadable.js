'use strict';

var GetIntrinsic = require('get-intrinsic');

var $isConcatSpreadable = GetIntrinsic('%Symbol.isConcatSpreadable%', true);

var Get = require('./Get');
var IsArray = require('./IsArray');
var ToBoolean = require('./ToBoolean');
var Type = require('./Type');

// https://ecma-international.org/ecma-262/6.0/#sec-isconcatspreadable

module.exports = function IsConcatSpreadable(O) ***REMOVED***
	if (Type(O) !== 'Object') ***REMOVED***
		return false;
	***REMOVED***
	if ($isConcatSpreadable) ***REMOVED***
		var spreadable = Get(O, $isConcatSpreadable);
		if (typeof spreadable !== 'undefined') ***REMOVED***
			return ToBoolean(spreadable);
		***REMOVED***
	***REMOVED***
	return IsArray(O);
***REMOVED***;
