'use strict';

var GetIntrinsic = require('get-intrinsic');

var $RangeError = GetIntrinsic('%RangeError%');

var ToInteger = require('./ToInteger');
var ToLength = require('./ToLength');
var SameValue = require('./SameValue');

// https://262.ecma-international.org/12.0/#sec-toindex

module.exports = function ToIndex(value) ***REMOVED***
	if (typeof value === 'undefined') ***REMOVED***
		return 0;
	***REMOVED***
	var integerIndex = ToInteger(value);
	if (integerIndex < 0) ***REMOVED***
		throw new $RangeError('index must be >= 0');
	***REMOVED***
	var index = ToLength(integerIndex);
	if (!SameValue(integerIndex, index)) ***REMOVED***
		throw new $RangeError('index must be >= 0 and < 2 ** 53 - 1');
	***REMOVED***
	return index;
***REMOVED***;
