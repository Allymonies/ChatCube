'use strict';

var GetIntrinsic = require('get-intrinsic');

var $TypeError = GetIntrinsic('%TypeError%');

var isNaN = require('../../helpers/isNaN');

var Type = require('../Type');

// https://262.ecma-international.org/11.0/#sec-numeric-types-number-add

module.exports = function NumberAdd(x, y) ***REMOVED***
	if (Type(x) !== 'Number' || Type(y) !== 'Number') ***REMOVED***
		throw new $TypeError('Assertion failed: `x` and `y` arguments must be Numbers');
	***REMOVED***

	if (isNaN(x) || isNaN(y) || (x === Infinity && y === -Infinity) || (x === -Infinity && y === Infinity)) ***REMOVED***
		return NaN;
	***REMOVED***

	if ((x === Infinity && y === Infinity) || (x === -Infinity && y === -Infinity)) ***REMOVED***
		return x;
	***REMOVED***

	if (x === Infinity) ***REMOVED***
		return x;
	***REMOVED***

	if (y === Infinity) ***REMOVED***
		return y;
	***REMOVED***

	if (x === y && x === 0) ***REMOVED***
		return Infinity / x === -Infinity && Infinity / y === -Infinity ? -0 : +0;
	***REMOVED***

	if (x === -y || -x === y) ***REMOVED***
		return +0;
	***REMOVED***

	// shortcut for the actual spec mechanics
	return x + y;
***REMOVED***;
