'use strict';

var GetIntrinsic = require('get-intrinsic');

var $TypeError = GetIntrinsic('%TypeError%');

var isNaN = require('../../helpers/isNaN');

var Type = require('../Type');

// https://262.ecma-international.org/11.0/#sec-numeric-types-number-unaryMinus

module.exports = function NumberUnaryMinus(x) ***REMOVED***
	if (Type(x) !== 'Number') ***REMOVED***
		throw new $TypeError('Assertion failed: `x` argument must be a Number');
	***REMOVED***
	if (isNaN(x)) ***REMOVED***
		return NaN;
	***REMOVED***
	return -x;
***REMOVED***;
