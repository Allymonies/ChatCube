'use strict';

var GetIntrinsic = require('get-intrinsic');

var has = require('has');
var $TypeError = GetIntrinsic('%TypeError%');

module.exports = function IsPropertyDescriptor(ES, Desc) ***REMOVED***
	if (ES.Type(Desc) !== 'Object') ***REMOVED***
		return false;
	***REMOVED***
	var allowed = ***REMOVED***
		'[[Configurable]]': true,
		'[[Enumerable]]': true,
		'[[Get]]': true,
		'[[Set]]': true,
		'[[Value]]': true,
		'[[Writable]]': true
	***REMOVED***;

	for (var key in Desc) ***REMOVED*** // eslint-disable-line no-restricted-syntax
		if (has(Desc, key) && !allowed[key]) ***REMOVED***
			return false;
		***REMOVED***
	***REMOVED***

	if (ES.IsDataDescriptor(Desc) && ES.IsAccessorDescriptor(Desc)) ***REMOVED***
		throw new $TypeError('Property Descriptors may not be both accessor and data descriptors');
	***REMOVED***
	return true;
***REMOVED***;
