'use strict';

var $isNaN = Number.isNaN || function (a) ***REMOVED*** return a !== a; ***REMOVED***;

module.exports = Number.isFinite || function (x) ***REMOVED*** return typeof x === 'number' && !$isNaN(x) && x !== Infinity && x !== -Infinity; ***REMOVED***;
