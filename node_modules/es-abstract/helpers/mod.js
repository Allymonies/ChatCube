'use strict';

var $floor = Math.floor;

module.exports = function mod(number, modulo) ***REMOVED***
	var remain = number % modulo;
	return $floor(remain >= 0 ? remain : remain + modulo);
***REMOVED***;
