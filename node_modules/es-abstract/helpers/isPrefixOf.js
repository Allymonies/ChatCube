'use strict';

var $strSlice = require('call-bind/callBound')('String.prototype.slice');

module.exports = function isPrefixOf(prefix, string) ***REMOVED***
	if (prefix === string) ***REMOVED***
		return true;
	***REMOVED***
	if (prefix.length > string.length) ***REMOVED***
		return false;
	***REMOVED***
	return $strSlice(string, 0, prefix.length) === prefix;
***REMOVED***;
