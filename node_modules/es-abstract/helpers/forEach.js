'use strict';

module.exports = function forEach(array, callback) ***REMOVED***
	for (var i = 0; i < array.length; i += 1) ***REMOVED***
		callback(array[i], i, array); // eslint-disable-line callback-return
	***REMOVED***
***REMOVED***;
