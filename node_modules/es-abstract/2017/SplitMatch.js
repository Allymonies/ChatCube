'use strict';

var GetIntrinsic = require('get-intrinsic');
var callBound = require('call-bind/callBound');

var $TypeError = GetIntrinsic('%TypeError%');

var IsInteger = require('./IsInteger');
var Type = require('./Type');

var $charAt = callBound('String.prototype.charAt');

// https://262.ecma-international.org/6.0/#sec-splitmatch

module.exports = function SplitMatch(S, q, R) ***REMOVED***
	if (Type(S) !== 'String') ***REMOVED***
		throw new $TypeError('Assertion failed: `S` must be a String');
	***REMOVED***
	if (!IsInteger(q)) ***REMOVED***
		throw new $TypeError('Assertion failed: `q` must be an integer');
	***REMOVED***
	if (Type(R) !== 'String') ***REMOVED***
		throw new $TypeError('Assertion failed: `R` must be a String');
	***REMOVED***
	var r = R.length;
	var s = S.length;
	if (q + r > s) ***REMOVED***
		return false;
	***REMOVED***

	for (var i = 0; i < r; i += 1) ***REMOVED***
		if ($charAt(S, q + i) !== $charAt(R, i)) ***REMOVED***
			return false;
		***REMOVED***
	***REMOVED***

	return q + r;
***REMOVED***;
