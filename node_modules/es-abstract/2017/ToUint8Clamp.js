'use strict';

var ToNumber = require('./ToNumber');
var floor = require('./floor');

var $isNaN = require('../helpers/isNaN');

// https://ecma-international.org/ecma-262/6.0/#sec-touint8clamp

module.exports = function ToUint8Clamp(argument) ***REMOVED***
	var number = ToNumber(argument);
	if ($isNaN(number) || number <= 0) ***REMOVED*** return 0; ***REMOVED***
	if (number >= 0xFF) ***REMOVED*** return 0xFF; ***REMOVED***
	var f = floor(argument);
	if (f + 0.5 < number) ***REMOVED*** return f + 1; ***REMOVED***
	if (number < f + 0.5) ***REMOVED*** return f; ***REMOVED***
	if (f % 2 !== 0) ***REMOVED*** return f + 1; ***REMOVED***
	return f;
***REMOVED***;
