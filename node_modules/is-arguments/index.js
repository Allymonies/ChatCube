'use strict';

var hasToStringTag = require('has-tostringtag/shams')();
var callBound = require('call-bind/callBound');

var $toString = callBound('Object.prototype.toString');

var isStandardArguments = function isArguments(value) ***REMOVED***
	if (hasToStringTag && value && typeof value === 'object' && Symbol.toStringTag in value) ***REMOVED***
		return false;
	***REMOVED***
	return $toString(value) === '[object Arguments]';
***REMOVED***;

var isLegacyArguments = function isArguments(value) ***REMOVED***
	if (isStandardArguments(value)) ***REMOVED***
		return true;
	***REMOVED***
	return value !== null &&
		typeof value === 'object' &&
		typeof value.length === 'number' &&
		value.length >= 0 &&
		$toString(value) !== '[object Array]' &&
		$toString(value.callee) === '[object Function]';
***REMOVED***;

var supportsStandardArguments = (function () ***REMOVED***
	return isStandardArguments(arguments);
***REMOVED***());

isStandardArguments.isLegacyArguments = isLegacyArguments; // for tests

module.exports = supportsStandardArguments ? isStandardArguments : isLegacyArguments;
