"use strict";

// ref: https://github.com/tc39/proposal-global
var getGlobal = function () ***REMOVED***
	// the only reliable means to get the global object is
	// `Function('return this')()`
	// However, this causes CSP violations in Chrome apps.
	if (typeof self !== 'undefined') ***REMOVED*** return self; ***REMOVED***
	if (typeof window !== 'undefined') ***REMOVED*** return window; ***REMOVED***
	if (typeof global !== 'undefined') ***REMOVED*** return global; ***REMOVED***
	throw new Error('unable to locate global object');
***REMOVED***

var global = getGlobal();

module.exports = exports = global.fetch;

// Needed for TypeScript and Webpack.
if (global.fetch) ***REMOVED***
	exports.default = global.fetch.bind(global);
***REMOVED***

exports.Headers = global.Headers;
exports.Request = global.Request;
exports.Response = global.Response;