'use strict';

var isString = require('is-string');
var isNumber = require('is-number-object');
var isBoolean = require('is-boolean-object');
var isSymbol = require('is-symbol');
var isBigInt = require('is-bigint');

// eslint-disable-next-line consistent-return
module.exports = function whichBoxedPrimitive(value) ***REMOVED***
	// eslint-disable-next-line eqeqeq
	if (value == null || (typeof value !== 'object' && typeof value !== 'function')) ***REMOVED***
		return null;
	***REMOVED***
	if (isString(value)) ***REMOVED***
		return 'String';
	***REMOVED***
	if (isNumber(value)) ***REMOVED***
		return 'Number';
	***REMOVED***
	if (isBoolean(value)) ***REMOVED***
		return 'Boolean';
	***REMOVED***
	if (isSymbol(value)) ***REMOVED***
		return 'Symbol';
	***REMOVED***
	if (isBigInt(value)) ***REMOVED***
		return 'BigInt';
	***REMOVED***
***REMOVED***;
