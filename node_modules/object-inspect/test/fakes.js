'use strict';

var inspect = require('../');
var test = require('tape');
var hasToStringTag = require('has-tostringtag/shams')();
var forEach = require('for-each');

test('fakes', ***REMOVED*** skip: !hasToStringTag ***REMOVED***, function (t) ***REMOVED***
    forEach([
        'Array',
        'Boolean',
        'Date',
        'Error',
        'Number',
        'RegExp',
        'String'
    ], function (expected) ***REMOVED***
        var faker = ***REMOVED******REMOVED***;
        faker[Symbol.toStringTag] = expected;

        t.equal(
            inspect(faker),
            '***REMOVED*** [Symbol(Symbol.toStringTag)]: \'' + expected + '\' ***REMOVED***',
            'faker masquerading as ' + expected + ' is not shown as one'
        );
    ***REMOVED***);

    t.end();
***REMOVED***);
