'use strict';

var inspect = require('../');
var test = require('tape');
var hasToStringTag = require('has-tostringtag/shams')();

test('bigint', ***REMOVED*** skip: typeof BigInt === 'undefined' ***REMOVED***, function (t) ***REMOVED***
    t.test('primitives', function (st) ***REMOVED***
        st.plan(3);

        st.equal(inspect(BigInt(-256)), '-256n');
        st.equal(inspect(BigInt(0)), '0n');
        st.equal(inspect(BigInt(256)), '256n');
    ***REMOVED***);

    t.test('objects', function (st) ***REMOVED***
        st.plan(3);

        st.equal(inspect(Object(BigInt(-256))), 'Object(-256n)');
        st.equal(inspect(Object(BigInt(0))), 'Object(0n)');
        st.equal(inspect(Object(BigInt(256))), 'Object(256n)');
    ***REMOVED***);

    t.test('syntactic primitives', function (st) ***REMOVED***
        st.plan(3);

        /* eslint-disable no-new-func */
        st.equal(inspect(Function('return -256n')()), '-256n');
        st.equal(inspect(Function('return 0n')()), '0n');
        st.equal(inspect(Function('return 256n')()), '256n');
    ***REMOVED***);

    t.test('toStringTag', ***REMOVED*** skip: !hasToStringTag ***REMOVED***, function (st) ***REMOVED***
        st.plan(1);

        var faker = ***REMOVED******REMOVED***;
        faker[Symbol.toStringTag] = 'BigInt';
        st.equal(
            inspect(faker),
            '***REMOVED*** [Symbol(Symbol.toStringTag)]: \'BigInt\' ***REMOVED***',
            'object lying about being a BigInt inspects as an object'
        );
    ***REMOVED***);

    t.end();
***REMOVED***);
