'use strict';

/* globals lockdown */

// requiring ses exposes "lockdown" on the global
require('ses');

// lockdown freezes the primordials
lockdown(***REMOVED*** errorTaming: 'unsafe' ***REMOVED***);

// initialize the module
require('./');
