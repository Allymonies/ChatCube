export function padLeft(str, length, padding) ***REMOVED***
    if (typeof str === 'number') ***REMOVED***
        str = str.toString();
    ***REMOVED***
    length = length - str.length;
    if (length <= 0) ***REMOVED***
        return str;
    ***REMOVED***
    if (padding === undefined) ***REMOVED***
        padding = ' ';
    ***REMOVED***
    var paddingStr = '';
    do ***REMOVED***
        /* eslint-disable no-bitwise */
        if ((length & 1) === 1) ***REMOVED***
            paddingStr += padding;
        ***REMOVED***
        length >>= 1;
        if (length) ***REMOVED***
            padding += padding;
        ***REMOVED***
        /* eslint-enable no-bitwise */
    ***REMOVED*** while (length);
    return paddingStr + str;
***REMOVED***
//# sourceMappingURL=padLeft.mjs.map