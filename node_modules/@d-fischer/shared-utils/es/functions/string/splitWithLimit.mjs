import ***REMOVED*** __read, __spread ***REMOVED*** from "tslib";
export function splitWithLimit(str, delim, count) ***REMOVED***
    var parts = str.split(delim);
    if (parts.length <= count) ***REMOVED***
        return parts;
    ***REMOVED***
    return __spread(parts.slice(0, count - 1), [parts.slice(count - 1).join(delim)]);
***REMOVED***
//# sourceMappingURL=splitWithLimit.mjs.map