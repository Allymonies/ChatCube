'use strict';

var test = require('tape');
var callBind = require('call-bind');

var allSettled = require('../implementation');
var runTests = require('./tests');

var bound = callBind(allSettled);

// eslint-disable-next-line no-shadow
var rebindable = function allSettled(iterable) ***REMOVED***
	// eslint-disable-next-line no-invalid-this
	return bound(typeof this === 'undefined' ? Promise : this, iterable);
***REMOVED***;

test('as a function', function (t) ***REMOVED***
	t.test('bad Promise/this value', function (st) ***REMOVED***
		// eslint-disable-next-line no-useless-call
		st['throws'](function () ***REMOVED*** allSettled.call(undefined, []); ***REMOVED***, TypeError, 'undefined is not an object');

		// eslint-disable-next-line no-useless-call
		st['throws'](function () ***REMOVED*** allSettled.call(null, []); ***REMOVED***, TypeError, 'null is not an object');
		st.end();
	***REMOVED***);

	runTests(rebindable, t);

	t.end();
***REMOVED***);
