'use strict';

var allSettled = require('..');
var test = require('tape');
var runTests = require('./tests');

test('as a function', function (t) ***REMOVED***
	t.test('bad Promise/this value', function (st) ***REMOVED***
		// below test is skipped, because for convenience, i'm explicitly turning `undefined` into `Promise` in the main export
		// eslint-disable-next-line no-useless-call
		// st['throws'](function () ***REMOVED*** any.call(undefined, []); ***REMOVED***, TypeError, 'undefined is not an object');

		// eslint-disable-next-line no-useless-call
		st['throws'](function () ***REMOVED*** allSettled.call(null, []); ***REMOVED***, TypeError, 'null is not an object');
		st.end();
	***REMOVED***);

	runTests(allSettled, t);

	t.end();
***REMOVED***);
